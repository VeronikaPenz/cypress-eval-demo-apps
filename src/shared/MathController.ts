export class MathController {
  add(v1: number, v2: number) { return v1 + v2; }
  subtract(v1: number, v2: number) { return v1 - v2; }
  multiply(v1: number, v2: number) { return v1 * v2; }
  divide(v1: number, v2: number) { return v1 / v2; }
}